apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ansible-builder-pvc
  namespace: ansible-ee-pipeline
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  volumeMode: Filesystem
